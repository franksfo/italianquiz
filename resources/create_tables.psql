-- idempotent
DROP TABLE verb;
DROP TABLE vgroup;

CREATE TABLE verb (id INTEGER, PRIMARY KEY(id), created TIMESTAMP DEFAULT now(), updated TIMESTAMP, 
		   value JSON);

CREATE TABLE vgroup (id INTEGER, PRIMARY KEY(id), created TIMESTAMP DEFAULT now(), updated TIMESTAMP, 
                     value JSON);

